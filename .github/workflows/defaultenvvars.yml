name: List all default env vars

on:
    workflow_dispatch

jobs:
    ubuntu-environment-variables:
        runs-on: ubuntu-latest
        steps:
          - run: env

    windows_environment-variable:
        runs-on: windows-latest
        steps:
          - run: env

    